<%= form_for(photographe) do |f| %>
  <% if photographe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(photographe.errors.count, "error") %> prohibited this photographe from being saved:</h2>

      <ul>
      <% photographe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nom_et_prenom %>
    <%= f.text_field :nom_et_prenom %>
  </div>

  <div class="field">
    <%= f.label :numero %>
    <%= f.text_field :numero %>
  </div>

  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>

  <div class="field">
    <%= f.label :equipe %>
    <%= f.text_field :equipe %>
  </div>

  <div class="field">
    <%= f.label :info_carriere %>
    <%= f.text_area :info_carriere %>
  </div>

  <div class="field">
    <%= f.label :ambition %>
    <%= f.text_area :ambition %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
