<p id="notice"><%= notice %></p>

<h1>Scriptes</h1>

<% @scriptes.each do |scripte| %>
                <div class="col-lg-3 col-md-6 col-12">
                    <!-- Single Package -->
                    <div class="single-package">
                        
                        <div class="trip-details">
                            <div class="left">
                                <h4>
                                    <a href="<%= scripte_path(scripte.id) %>"><%= scripte.nom_et_prenom %></a>
                                </h4>
                                <p><%= scripte.numero %></p>
                            </div>
                            <div class="right">
                               
                                    <p><%= scripte.email %></p>
                            </div>
                            <a href="<%= scripte_path(scripte.id) %>" class="btn primary">Plus de détail</a>
                        </div>
                    </div>
                    
                </div>
            <% end %>

<br>

<%= link_to 'New Scripte', new_scripte_path %>
