<p id="notice"><%= notice %></p>

<h1>Scriptes</h1>

<% @scriptes.each do |scripte| %>
                <div class="col-lg-3 col-md-6 col-12">
                    <!-- Single Package -->
                    <div class="single-package">
                        
                        <div class="trip-details">
                            <div class="left">
                                <h4>
                                    <%= scripte.nom_et_prenom %>
                                </h4>
                                <p><%= scripte.numero %></p>
                            </div>
                            <div class="right">
                               
                                    <p><%= scripte.email %></p>
                            </div>
                            <a href="<%= scripte_path(scripte.id) %>" class="btn primary">Plus de détail</a>
                        </div>
                    </div>
                    
                </div>
            <% end %>

<br>

<div class="buttn"><%= link_to 'New Scripte', new_scripte_path %></div>
