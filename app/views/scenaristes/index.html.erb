<p id="notice"><%= notice %></p>

<h1>Scenaristes</h1>

<% @scenaristes.each do |scenariste| %>
                <div class="col-lg-3 col-md-6 col-12">
                    <!-- Single Package -->
                    <div class="single-package">
                        
                        <div class="trip-details">
                            <div class="left">
                                <h4>
                                    <%= scenariste.nom_et_prenom %>
                                </h4>
                                <p><%= scenariste.numero %></p>
                            </div>
                            <div class="right">
                               
                                    <p><%= scenariste.email %></p>
                            </div>
                            <a href="<%= scenariste_path(scenariste.id) %>" class="btn primary">Plus de détail</a>
                        </div>
                    </div>
                    
                </div>
            <% end %> 

<br>

<div class="buttn"><%= link_to 'New Scenariste', new_scenariste_path %></div>
